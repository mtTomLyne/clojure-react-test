Once downloaded, open the folder in VS code and then open a terminal in the folder

Then run

```
npm install
npm start
```

This will install the dependencies and then start the app on localhost:3000